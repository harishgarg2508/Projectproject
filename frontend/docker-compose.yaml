services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules 

    networks:
      - shared-network

networks:
  shared-network:
    external: true
